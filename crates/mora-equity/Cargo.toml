[package]
name = "mora-equity"
version = "0.1.0"
edition = "2021"
license = "Apache-2.0"
description = "The equity layer: xStock positions, and the 13F filing threshold proved without revealing the portfolio"

[dependencies]
aperture-core = { git = "https://github.com/psyto/aperture", tag = "v0.5.1" }
mora-embargo = { path = "../mora-embargo" }
solana-zk-sdk = "7"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
