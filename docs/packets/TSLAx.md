# Collateral admission packet — TSLAx on Kamino

*Tesla xStock · Backed · generated by `./scripts/packet.sh TSLAx`, from mainnet, at 2026-09-20 11:51:45 UTC.*

Generated rather than authored. **The day it disagrees with the chain is the day someone changed
something**, not the day this went stale. Every unknown below produces a cap of `$0` on its own
line rather than a number nobody can defend.

## 0. The question this answers

Not *should Kamino lend against TSLAx* — it already does. The reserve exists, and someone chose
its numbers. The question is the narrower one:

> **A holder who will not post this collateral publicly — what would it take to let them post it
> at all?**

## 1. The asset

| | |
|---|---|
| mint | `XsDoVfqeBukxuZHWhdvWHBhgEHjGNst4MLodqsJHzoB` |
| issuer | Backed |
| token program | Token-2022 |
| decimals | 8 |
| supply | 229,636.85 TSLAx |
| extensions | `confidentialTransferMint`, `defaultAccountState`, `metadataPointer`, `pausableConfig`, `permanentDelegate`, `scaledUiAmountConfig`, `tokenMetadata`, `transferHook` |

**Legal claim, redemption route and holder eligibility are not on-chain and are not asserted
here.** They are the issuer's to state, they materially change the recovery value, and a packet
that guessed at them would be worse than one that leaves them open. → **§6**

## 2. Every authority on the token, and who holds it

| holder | what it can do |
|---|---|
| `5aMNNLQJwAEeoemTEMkv5NVjqKwvvefRYCQ5Z67HFvEq` | **move any holder's tokens, without their signature**<br>install a transfer hook (none set today)<br>approve confidential accounts, set the auditor key |
| `JDq14BWvqCRFNu1krb12bcRpbGtJZ1FLEakMw6FdxJNs` | freeze any holder's account<br>pause all transfers of the token |
| `7pt9tkctJPK7PPNQJ77GKg8ZffSF6QxoMiCFYHxrtaCj` | mint new supply |
| `S7vYFFWH6BjJyEsdrPQpqpYTqLTrPRK6KW3VwsJuRaS` | change the UI multiplier — a stock split, applied to every balance |


## 3. Reserve evidence

| | |
|---|---|
| an attested proof of reserves for this mint | **not established here** |
| signer, freshness, coverage | **unknown** |
| independently verifiable | **unknown** |

**Contribution to the proposed cap: `$0`.** Not an accusation — this repository has not looked
for Backed's attestation, and the absence of a search is not the absence of evidence. It is a line a
risk owner has to fill from the issuer, and until it is filled it caps the number.

## 4. The oracle path, as the reserve is configured today

| | |
|---|---|
| source | **Scope**, feed `3t4JZcueEzTbVP6kLxXrL3VpWx45jDer4eqysweBchNH`, chain index 338 |
| Pyth fallback | `11111111111111111111111111111111` |
| Switchboard fallback | `11111111111111111111111111111111` |
| staleness tolerance | **300 s** on the price, 300 s on the TWAP |
| TWAP divergence limit | 500 bps |
| accepted price range | **300.00 – 520.00** in the oracle's own price unit — outside it the price is refused |

A single source with no fallback means an oracle outage is a freeze, not a degradation. The
price band is a real guard and also a real constraint: a move outside it stops the reserve rather
than repricing it.

**The band is in the oracle's price unit, and this packet has not established that unit is USD.**
Doing that means decoding the lending market's quote currency and what Scope feed index 338
denotes. Until then the numbers above are a range, not dollars.

**Confide does not change this line.** A floor proof says how many tokens are there; it says
nothing about what one is worth, and the packet should not pretend otherwise.

## 5. The parameters, already chosen

| | today |
|---|---|
| reserve | `5iTiczqgUegqA3PpoNpotizMbY9n1sRWr3oL6igKvWuf` |
| market | `5wJeMrUYECGq41fxRESKALVcHnNX26TAWy4W98yULsua` |
| status | Active |
| LTV | **55 %** |
| liquidation threshold | **65 %** |
| liquidation bonus | 5.00 % – 10.00 % |
| deposit cap | **20,000 TSLAx** |
| borrow limit | 2,500 TSLAx |
| currently deposited | 7,486.98 |


At the reserve's own price of 364.33, that cap and LTV authorise **$4,007,630 of borrowing** against this
token, of which **$0 is reachable while the position stays confidential**.

Across every Kamino reserve holding a tokenized stock, `./scripts/capacity.sh` puts that at
**$23.2 m deposited and $84.0 m of authorised borrowing, none of it reachable
confidentially** — read 2026-09-20 11:50:46 UTC.

**Nothing in this packet proposes changing any of them.** They are the prior, and the whole
point of the request below is that it does not need them moved.

## 6. What a confidential position would need, and what it costs

Kamino refuses a token account that can hold value confidentially — `constraints.rs:187` and
`:201`, on the holder's own account, on deposit, borrowing and both sides of liquidation. So the
holder's options today are to post publicly or not to post. See [`../KAMINO.md`](../KAMINO.md).

| | state | who |
|---|---|---|
| A **floor**, proved — a lower bound on the balance, so the reserve values the position at `floor × price` and ignores the rest | **runs** | Confide |
| **Custody that survives the holder** — the escrow's owner is a program PDA before anything is recorded | **runs** | Confide |
| **Re-proving** — a floor proved once is a floor at one moment; a reserve marks continuously | **not built** | Confide |
| **Liquidation hand-off** — the seizure has to sit inside Kamino's liquidation instruction, not beside it | **not built** | Confide, then Kamino |
| **An exemption** for a reserve-recognised escrow from `:187` and `:201` | **not asked** | Kamino |

**Cost to integrate, honestly bounded:** the exemption is a change to a constraint function and
its tests. The hand-off is not — it touches the liquidation path, which is the part of a lending
program where a mistake is a loss rather than a bug. **This packet does not claim that cost is
small.** It claims the first two lines are done and the rest is specifiable.

## 7. What would make this refusable, and rightly

- The reserve evidence line in §3 stays unknown. → cap `$0`, and correctly.
- **The issuer will not approve the escrow.** This mint sets `autoApproveNewAccounts: false`, so
  a freshly configured confidential account is inert until the confidential-transfer mint
  authority approves it — **once per account, by Backed**.

  **Once per account, not once per loan.** `SetAuthority` moves an approved account to a loan
  PDA and leaves the approval in place, verified on devnet 2026-09-19, so a holder who already
  holds this token confidentially can pledge that account without asking again. What they give
  up is the whole account: pledging part of a position needs a second one, and a second one
  needs Backed.

  It is still the most concrete blocker in the
  document and it is not a Kamino decision at all. Confide's own demo hits it and has to add an
  approve step. **An integration that assumed away issuer participation would be wrong.**

  **And it is the same field Kamino requires.** `constraints.rs:131` refuses a liquidity mint
  whose `auto_approve_new_accounts` is *true* — *"Auto approve new accounts must be false for
  liquidity tokens"*. So the setting that admits this mint to Kamino at all is the setting that
  puts Backed in the path of every escrow. The two requirements are one field read from two
  sides, and satisfying either one forces the other.

  Measured across the whole asset class by `./scripts/slot-scan.sh`: **1,992 of 1,992
  tokenized-equity mints on Solana set it false. Zero auto-approve.** There is no mint on the
  ungated side, so this is not solved by choosing a different ticker, and **no engineering on
  Confide's side removes it** — the floor can be proved, the collateral locked and the default
  settled without the holder, and the position still cannot exist, because the account it would
  live in cannot be opened without the issuer.
- **Freeze and pause are judged too much control for a position the lender cannot see.** A frozen
  or paused token cannot be transferred, so it cannot be liquidated either, and the holder of
  that authority is in §2. **That is a coherent refusal and the packet has no answer to it.**

  *A permanent delegate is the wrong version of this objection and an earlier draft made it. A
  permanent delegate can move a **public** balance; a confidential withdrawal validates the
  account owner and needs proofs bound to that account's ElGamal key, so it is not a demonstrated
  power to move an invisible balance. Freeze and pause are the real ones.*
- The liquidation hand-off proves not to fit inside Kamino's instruction without a change nobody
  wants to make to the liquidation path.
- Nobody wants to hold a confidential position badly enough to be worth the work. **No evidence
  is offered here that anyone does.**

---

*Reproduce: `./scripts/packet.sh TSLAx`. The chain reads are live; the klend rules and layout are
pinned at `release/v1.25.0` and re-checked by `./scripts/kamino-verdict.sh`.*
