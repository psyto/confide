# Should Confide change what it is, with 23 days left? Attack this.

Written 2026-09-19. Submission closes 2026-10-12 23:59 PT. The founder has asked for the
fundamental move rather than the next task and has explicitly accepted risk to be far ahead of
other teams. **This proposal was written by the same agent that did the work it proposes to
extend, so it is not safely reviewed by its author.**

Read these in the repository; do not rely on this summary:

- `docs/cwf-2026/THE-SWING.md` — the proposal itself, with its own case-against section.
- `docs/cwf-2026/CRITERIA.md` — both judging lists, with sources. §8 was read from the PDF.
- `docs/27-DAYS.md` — the plan, with today's correction at the top and two retired bets in it.
- `programs/confide-seizure/src/lib.rs` — what actually exists: `originate`, `seize`, `deshield`
  (disabled), `in_default`, `may_seize`, `floor_is_proved`, `context_is_armed`.
- `scripts/seizure-e2e.sh`, `scripts/prove-collateral.sh`, `docs/SEIZURE.md`.
- `docs/KAMINO.md` and `scripts/kamino-verdict.sh` — the `REQUIRES INTEGRATION` verdict.
- Your pass from yesterday: `docs/reviews/2026-09-18-codex-what-wins.md`, **including the
  verification section appended to it.** Your first finding there was wrong: all eight line
  citations are correct, checked with `sed` against the clone at the pinned commit. That is the
  second time you have missed on line numbers inferred from GitHub anchors. Please clone and read.

## The proposal in one sentence

Stop being a report about Kamino's limitation. Build the complete confidential-collateral loan
lifecycle — originate, re-prove, repay, default, seize — on devnet, behind a page a judge can
operate, so `REQUIRES INTEGRATION` becomes the setup rather than the conclusion.

## The questions, hardest first

1. **Is the premise correction load-bearing or am I overreacting to it?** The plan's opening
   sentence — "twenty-seven days of more mechanism does not move that" — rested on traction being
   read first. Traction is listed last and is absent from Official Rules §8. Does that actually
   justify reversing the month's direction, or is it a technicality being used to do the fun thing?

2. **Is `repay` feasible in 23 days, and is it the same wall `deshield` hit?** `deshield` is
   disabled because a public balance in a PDA-owned account is not movable by an ordinary transfer —
   recorded as a *design* error, not a coding one. My claim is that `repay` is shaped like `seize`,
   which works: a confidential transfer out of a PDA-owned escrow authorised by the program, with a
   different destination and precondition. **Read `seize` and tell me if that is true.** If it is
   not, the whole proposal collapses and I would rather know now.

3. **Does a reference venue read as scope creep?** This repository's character is refusing to
   overclaim — refusal lists, dated reversals, claims deleted when they could not be supported.
   Building our own venue to escape our own `BLOCKED` verdict could read as escaping the verdict.
   Is there a framing that survives a judge who liked the discipline, or does the act itself damage
   the thing that made this distinctive?

4. **What should be cut to pay for it?** Required and unbuilt: narrated presentation video, demo
   video (≤3 min), product logo, GTM / demand validation / distribution, form text. My claim is
   that a judge cannot score what was not submitted, so those come first or in parallel and the
   build is what slips. Is that ordering right, or is one of those deliverables worth less than I
   think?

5. **Is there a better swing?** I considered and rejected: going to a venue that already permits
   this (I could not find a Solana one where a curator is not still required); making the holder the
   user (that is a disclosure tool, which is the pre-existing `aperture`); and leaving the decision
   page as the product (thin). **If there is a fundamentally different move, say it** — including
   "do not swing, finish the submission properly," if that is the honest answer.

6. **Timing of the public post.** No individual outreach, by founder ruling 2026-09-18; public
   posting is permitted. My claim is it must go out early because arrivals need lead time. Does an
   early post risk anything — being scooped, being read as attacking Kamino, or being judged on a
   number that then does not move?

**Say what you could not verify, and clone before citing any line.**
