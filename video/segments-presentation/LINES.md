# The CWF presentation, rough cut — the cut, clip by clip

Eight clips in `segments-presentation/`, **silent**, cut at the boundaries the recorder logged.
Generate the voice per clip and lay it back in order; the clip length is the budget, and the
delivery should finish a little before the picture does.

Generated by `python3 video/lines.py presentation` from [`../CWF-PRESENTATION.md`](../CWF-PRESENTATION.md) and
`manifest.json` — neither the lengths nor the lines are typed here.

| clip | length | words | pace |
|---|---|---|---|
| `01-what-this-is.mp4` | 8.0s | 17 | 138 wpm |
| `02-the-trade.mp4` | 23.0s | 52 | 139 wpm |
| `03-why-bother.mp4` | 16.0s (2s of it silent) | 31 | 139 wpm |
| `04-this-account.mp4` | 16.0s (5s of it silent) | 23 | 133 wpm |
| `05-already-shipped.mp4` | 15.0s | 33 | 138 wpm |
| `06-so-i-counted.mp4` | 12.0s (2s of it silent) | 22 | 140 wpm |
| `07-not-only-equities.mp4` | 15.0s | 32 | 133 wpm |
| `08-why-not-an-exchange.mp4` | 26.0s | 58 | 137 wpm |
| `09-what-confide-is.mp4` | 25.1s | 55 | 135 wpm |
| `10-what-is-missing.mp4` | 19.0s | 41 | 134 wpm |

Pace is words over the clip minus 0.6s of tail and any declared silence. Over 180 wpm is
reading without pauses.

## `01-what-this-is.mp4` — what this is

> Confide settles tokenized stock against a stablecoin in a single transaction, and neither side
> publishes what moved.

## `02-the-trade.mp4` — the trade

> Delivery, and payment, in one transaction. Neither could have happened without the other, which
> is the only thing a clearing house is for — and there isn't one. Confide holds nobody's assets
> and stands between nobody. It is what the two of them run to build the trade and to check it.

## `03-why-bother.mp4` — why anyone would bother

> So why hide it. If you hold tokenized stocks on Solana, everyone can see what you hold. Nobody
> attacked anything — the chain simply publishes it, the moment each purchase settles.

## `04-this-account.mp4` — this account

> This is a real account on Solana, right now. The chain says it holds nothing.
> 
> It holds a hundred and seventy-three thousand shares.

## `05-already-shipped.mp4` — already solved, already switched off

> Nearly two thousand of them, from three issuers with nothing to do with each other. Every one
> leaves the auditor key empty, and every one needs the issuer's signature to open an account.

## `06-so-i-counted.mp4` — so I counted

> So I stopped reading the settings and counted. Three hundred and twenty-nine thousand accounts,
> across Apple, NVIDIA, SpaceX and Anthropic.
> 
> Not one.

## `07-not-only-equities.mp4` — and it is not only equities

> That dollar has the same empty auditor slot and the same locked door. Two regulated issuers, two
> different asset classes, the same dead end. This was never a story about tokenized stocks.

## `08-why-not-an-exchange.mp4` — why not just use an exchange

> You cannot do this on one. A pool's reserves are public, and a trade moves them by exactly the
> amount traded — so anything settled against a pool publishes the size, whatever the token can do.
> It has to be two parties, directly. That is not a gap here; it is why this is shaped as it is.

## `09-what-confide-is.mp4` — so what Confide actually is

> The chain will not assemble that trade for you. The proofs do not fit in a transaction, and on a
> mint that charges a fee one of them does not fit at all. Confide builds them, puts them on chain,
> and hands each side the other's amount to decrypt before signing. That is the product.

## `10-what-is-missing.mp4` — what is missing

> Nobody outside this repository has used any of it. But what is left is not unknown: each obstacle
> is a condition you can check, and for the two that decide the market, the work on the far side is
> already running.
