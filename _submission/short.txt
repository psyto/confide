The SEC built the tape for tokenized equity. Nobody built the block — a stock-to-stablecoin swap in one Solana transaction, neither side publishing the size.

The SEC built the tape for tokenized equity. Nobody built the block — a stock-to-stablecoin swap in one Solana transaction, neither side publishing the size.
