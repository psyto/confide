[package]
name = "confide-seizure"
version = "0.1.0"
edition = "2021"
license = "Apache-2.0"
description = "Take confidential collateral on default, using proofs the borrower built when they still wanted the loan"

[lib]
crate-type = ["cdylib", "lib"]

[dependencies]
solana-program = "2.3"
spl-token-2022-interface = "3"
spl-token-confidential-transfer-proof-extraction = "0.6"
bytemuck = "1"
solana-address = "2"

[workspace]
